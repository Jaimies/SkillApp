<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.jdevs.timeo.ui.overview.OverviewViewModel" />
    </data>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="none"
        tools:context=".ui.overview.OverviewFragment">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/padding_default">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/section_background"
                android:onClick="@{() -> viewModel.navigateToProjects()}"
                android:orientation="vertical"
                android:paddingTop="@dimen/padding_default">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="@dimen/margin_small"
                    android:paddingStart="@dimen/padding_default"
                    android:paddingEnd="@dimen/padding_default"
                    android:text="@string/projects"
                    android:textAppearance="@style/TextAppearance.Timeo.Headline2" />

                <View style="@style/Widget.Timeo.Divider" />

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/projects_list"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:theme="@style/ThemeOverlay.Timeo.ListItem"
                    app:hideIf="@{viewModel.areProjectsEmpty}"
                    tools:itemCount="3"
                    tools:listitem="@layout/projects_item" />

                <include
                    android:id="@+id/projects_preloader"
                    layout="@layout/preloader"
                    app:hideIf="@{!viewModel.areProjectsLoading}" />

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:gravity="center"
                    android:orientation="vertical"
                    app:hideIf="@{!viewModel.areProjectsEmpty}"
                    tools:visibility="gone">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/there_is_nothing_here_yet"
                        android:textAppearance="@style/TextAppearance.MaterialComponents.Body1" />

                    <com.google.android.material.button.MaterialButton
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="@dimen/margin_default"
                        android:text="@string/create_project" />
                </LinearLayout>
            </LinearLayout>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/margin_default"
                android:background="@drawable/section_background"
                android:onClick="@{() -> viewModel.navigateToActivities()}"
                android:orientation="vertical"
                android:paddingTop="@dimen/padding_default"
                app:hideIf="@{!viewModel.activitiesEnabled}">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="@dimen/margin_small"
                    android:paddingStart="@dimen/padding_default"
                    android:paddingEnd="@dimen/padding_default"
                    android:text="@string/activities"
                    android:textAppearance="@style/TextAppearance.Timeo.Headline2" />

                <View style="@style/Widget.Timeo.Divider" />

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/activities_list"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:theme="@style/ThemeOverlay.Timeo.ListItem"
                    app:hideIf="@{viewModel.areActivitiesEmpty}"
                    tools:itemCount="3"
                    tools:listitem="@layout/activities_item" />

                <include
                    android:id="@+id/activities_preloader"
                    layout="@layout/preloader"
                    app:hideIf="@{!viewModel.areActivitiesLoading}" />

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:gravity="center"
                    android:orientation="vertical"
                    android:paddingTop="@dimen/padding_default"
                    android:paddingBottom="@dimen/padding_default"
                    app:hideIf="@{!viewModel.areActivitiesEmpty}"
                    tools:visibility="gone">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/there_is_nothing_here_yet"
                        android:textAppearance="@style/TextAppearance.MaterialComponents.Body1" />

                    <com.google.android.material.button.MaterialButton
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="@dimen/margin_small"
                        android:text="@string/create_activity" />
                </LinearLayout>
            </LinearLayout>
        </LinearLayout>
    </androidx.core.widget.NestedScrollView>
</layout>